TODO

x Update PCAP and Trace name
- Define IPv4 with and without TCP/UDP (can be updated later)
x TCP/UDP for encapsulated data
x Calculate the pkt sizes for UDP/TCP encapsulated
- ADD more protocols (ICMP, ARP...)
x Set the correct values information for the tracefiles
x Add the tracefiles setup for MACSAD use case
x Create pkts with the "-pkt" values
G create pkts with an opcional "data" set by the user
F when creating PCAP with same name, recreate the trace file instead of appending to it.
- create packet using a custom list of headers
F Performance flag
